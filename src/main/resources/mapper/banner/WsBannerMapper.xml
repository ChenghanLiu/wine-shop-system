<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wineshop.banner.mapper.WsBannerMapper">

    <resultMap id="BaseMap" type="com.wineshop.banner.entity.WsBanner">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="imageUrl" column="image_url"/>
        <result property="linkUrl" column="link_url"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="BaseColumns">id, title, image_url, link_url, sort, status, created_at, updated_at</sql>

    <select id="selectAll" resultMap="BaseMap">
        select <include refid="BaseColumns"/>
        from ws_banner
        order by sort asc, id desc
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into ws_banner(title, image_url, link_url, sort, status)
        values (#{title}, #{imageUrl}, #{linkUrl}, #{sort}, #{status})
    </insert>

    <update id="updateById">
        update ws_banner
        set title = #{title},
            image_url = #{imageUrl},
            link_url = #{linkUrl},
            sort = #{sort},
            status = #{status}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from ws_banner where id = #{id}
    </delete>

</mapper>
